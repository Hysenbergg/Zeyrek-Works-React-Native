import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        margin: 10,
        alignItems: 'center',
    },
    title: {
        color: 'white',
        backgroundColor: '#EF5350',
        borderRadius: 8,
        fontSize: 20,
        padding: 5
    },
})